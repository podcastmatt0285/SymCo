__pycache__/
*.pyc
*.pyo
*.db
*.sqlite
.vapid_keys.json
venv/
.venv/

# Android build output — regenerated from scratch on every build
android/twa-project/

# Runtime state files
tick_state.txt
widget_devices.json
Task

# Dev-only / scratch files
cheat.txt
*_backup.sql

# Claude helper session files — never commit these
claudeHelper/

# Keep intentional DB backup SQL files tracked (written by backup.sh)
!backups/*.sql
!backups/.gitkeep
